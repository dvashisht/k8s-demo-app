name: Build and Deploy

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    # Prevent infinite loop when workflow commits changes
    if: "!contains(github.event.head_commit.message, 'Update image')"

    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    # Login to Docker Hub
    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    # Build Docker image
    - name: Build Docker image
      run: |
        docker build -t ${{ secrets.DOCKER_USERNAME }}/demo-app:${{ github.sha }} .

    # Push Docker image
    - name: Push Docker image
      run: |
        docker push ${{ secrets.DOCKER_USERNAME }}/demo-app:${{ github.sha }}

    # Update Kubernetes manifest with new image
    - name: Update deployment.yaml
      run: |
        sed -i "s|image: .*|image: ${{ secrets.DOCKER_USERNAME }}/demo-app:${{ github.sha }}|" k8s/deployment.yaml

    # Commit and push changes back to repo
    - name: Commit updated manifest
      run: |
        git config user.name "github-actions"
        git config user.email "actions@github.com"
        git add k8s/deployment.yaml
        git commit -m "Update image to ${{ github.sha }}" || echo "No changes to commit"
        git push